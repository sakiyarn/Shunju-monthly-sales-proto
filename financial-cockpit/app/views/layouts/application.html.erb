<!DOCTYPE html>
<html>
  <head>
    <title data-inertia><%= content_for(:title) || "Financial Cockpit" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= inertia_meta_tags %>
    <%= vite_client_tag %>
    <%= vite_javascript_tag "application" %>
    <%= inertia_ssr_head %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
